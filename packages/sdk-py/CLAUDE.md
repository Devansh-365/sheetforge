# packages/sdk-py

## Purpose
Python SDK for ACID-ish Sheets (PyPI package: `acid-sheets`). Deferred to V1 — placeholder directory only.

## Public API
Not yet defined. V1 scope.

## Key Files
- None yet. Directory is a placeholder.

## Gotchas
- Do not implement anything here until V1 is planned and scoped.
- Python packaging will use pyproject.toml when implemented.

## Never Do
- Don't implement V1 Python SDK during V0 work.
- Don't add SaaS-specific secrets or internal URLs (OSS package).
