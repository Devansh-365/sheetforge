{
  "name": "@acid-sheets/slice-schema",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "lint": "biome check .",
    "typecheck": "tsc --noEmit"
  }
}
