{
  "name": "@acid-sheets/worker",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "echo '[apps/worker] not implemented yet; queue consumer lands in a later wave'",
    "build": "echo '[apps/worker] nothing to build yet'",
    "lint": "biome check .",
    "typecheck": "tsc --noEmit"
  }
}
